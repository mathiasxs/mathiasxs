### Mathias Schütz

Senior software developer. Angular and TypeScript by trade, currently moving into the backend.

Eight years of enterprise content management software: first as the only developer in a
regional subsidiary, where a one-off for a single customer turned into a product that sold
five times over, then in Berlin on a web client used by around half a million people, and
now on the admin applications replacing a Windows toolset grown since the 1990s. Angular is
where I make the calls — repository structure, going module-free, and the occasional
well-argued no. What colleagues pull me into is rarely the framework question, though. It is
the one underneath: whether a solution fits the way the product actually gets used.

Most of the code my team ships is now written with Claude Code. I set it up for us, wrote
the first CLAUDE.md, and I review and answer for every line that goes out. The speed is not
the interesting part. Keeping an architecture coherent when typing is no longer the
bottleneck — that is.

**Currently:** Java and Spring Boot, and building things with LLM APIs on the side.
**Elsewhere:** [LinkedIn](LINKEDIN-URL-HIER-EINTRAGEN)
